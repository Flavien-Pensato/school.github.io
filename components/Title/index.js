import React from 'react';
import { Flex, Heading } from 'theme-ui';

const Title = () => (
  <Flex sx={{ justifyContent: "center" }} my={[3, 4]}>
    <Heading as='h1'>MFR Chatte</Heading>
  </Flex>
);

export default Title;
